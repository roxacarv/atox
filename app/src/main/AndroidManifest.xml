<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.atox">

    <!--Internet Permission-->
    <uses-permission android:name="android.permission.INTERNET" />

    <!-- To auto-complete the email text field in the login form with the user's emails -->
    <uses-permission android:name="android.permission.GET_ACCOUNTS" />
    <uses-permission android:name="android.permission.READ_PROFILE" />
    <uses-permission android:name="android.permission.READ_CONTACTS" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="AIzaSyBh5DX_Oyt1GnmzcW1JIrYhnEkfkYr3tng" />

        <!-- SPLASH SCREEN -->
        <activity
            android:name=".navegacao.SplashActivity"
            android:theme="@style/SplashTheme">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- LOGIN SCREEN (COM AS ALTERAÇÕES DE RODRIGO) -->


        <!-- ONBOARD SCREEN WITH THE FRAGMENTS -->
        <activity android:name=".navegacao.OnboardingActivity" />

        <!-- THE CUSTOM LOGIN SCREEN -->
        <activity
            android:name=".usuario.gui.LoginActivity"
            android:theme="@style/FullScreenTheme" />

        <!-- REGISTER SCREEN -->
        <activity
            android:name=".usuario.gui.RegistroActivity"
            android:theme="@style/FullScreenTheme" />

        <!-- ENDERECO SCREEN -->
        <activity
            android:name=".usuario.gui.EnderecoActivity"
            android:theme="@style/FullScreenTheme" />

        <!-- BOTTOM MENU SCREEN -->
        <activity
            android:name=".navegacao.MenuActivity"
            android:theme="@style/FullScreenWithStatusBar"
            android:label="@string/title_activity_menu"></activity>
    </application>

</manifest>